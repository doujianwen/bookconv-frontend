# AI 自动化运营落地方案 v2.1

> 2026-07-30 · 基于对现有自动化的实地诊断与地基修复实践重写
> 替代 `ai-automation-plan-v2.md`（v2 方向正确但脱离地基，本版拉回地面）

---

## 一、为什么需要 v2.1

v2 方案的思维框架（闭环、分级、KPI）是对的，但存在三类致命问题：

1. **架构野心与地基现实脱节**：v2 描绘的是 Kafka/Pulsar + 强化学习 + 向量库 RAG + K8s 自动伸缩的 AIOps 蓝图。但实地诊断发现，现状是审计脚本连 `require('fs')` 都缺、用 `Math.random()` 冒充队列长度、日志换行都是错的。
2. **核心创新是技术黑箱**："认知纠正代理"宣称做根因分析（依赖图/传播模型）+ 强化学习决策 + 向量库 + 模型迭代，但通篇没说用什么实现。这是大厂 AIOps 团队几年的活，远超一人公司电子书站的能力边界。
3. **混淆工程自愈与业务增长**：把低风险的内容生成和高风险的版本回滚塞进同一个"大脑"统一调度，既不必要也危险。

本次实地诊断（2026-07-30）用证据印证了上述判断：

- `ai-audit.js` 有 **3 份副本**（1 份 UTF-16 残缺、1 份 3 字节空文件、1 份完整但路径全错）。
- 日志 `logs/ai-operation.txt` 里的 `Queue: 21/16/8` 全是 `Math.floor(Math.random()*30)` 伪造的——**系统一直在用假数据骗自己**。
- `Success rate: 95%` 是找不到日志时的默认 fallback，不是真实统计。
- `BLOG_DIR` 路径双重 `ebook-converter`，永远找不到博客目录，所以早期日志 `Blog:0`。

地基已先行修复（见第三节），v2.1 在真实地基上重新规划。

---

## 二、设计原则

1. **不自欺**：数据源不可用时如实报告 `unknown`，绝不伪造。所有告警必须基于真实数据。这是 v2.1 与 v2 最大的区别。
2. **务实够用**：复杂度与项目生命周期和重要性严格匹配。一人公司的电子书转换站，不需要 Kafka。用最小可行技术栈解决问题。
3. **解耦**：工程稳定性（传统 SRE 监控）与业务增长（内容/邮件流水线）是两条独立线，不强行统一抽象。
4. **先基线后目标**：KPI 先测当前值再设目标。没有基线的"+15%/月"是空中楼阁。
5. **人工兜底**：所有高风险操作（删数据、回滚版本）必须人工确认。代理只建议，不执行。

---

## 三、已完成的地基修复（2026-07-30）

| 修复项 | 修复前 | 修复后 |
|--------|--------|--------|
| ai-audit.js 副本混乱 | 3 份（残缺/空/路径错） | 唯一正本 `ebook-converter/scripts/ai-audit.js`；3 份垃圾移入 `_ebook_converter_junk_2026-07-29/automation-cleanup-2026-07-30/` 可恢复 |
| BLOG_DIR 路径 | 双重 `ebook-converter`，永远找不到 | 修正为 `../src/app/blog`，实测数到 7 篇 |
| Redis 队列 | 连不上时 `Math.random()` 伪造 | 如实报告 `unknown`，状态降为 `warning` |
| Success rate | `95%` 无标注 | 标注 `（假设值，无日志）` |
| 日志换行 | 字面 `\n` 糊成一行 | 真换行多行输出 |
| webhook | 硬编码在源码 | 外置到 `.feishu-webhook`（已加 .gitignore），`FEISHU_WEBHOOK` 环境变量可覆盖 |
| generate-blogs.js `date` bug | `date: new Date().toISOString()` 字符串不求值，MDX 非法日期 | 改为模板插值，实测生成真实 ISO 时间戳 |
| generate-blogs.js 硬编码路径 | `"E:\\一人公司\\..."` 不可移植 | 改为 `path.join(__dirname, "..", "..")` 相对路径 |
| app 无结构化转换日志 | logger 只输出 stdout 不写文件，成功率永远 `assumed 95%` | `queue.ts` 加 `appendConversionAuditLog` 写 `logs/conversion.log`，格式匹配 ai-audit.js 正则；实测 67% 真实统计 |
| 调度缺失 | ai-audit.js 无任何定时调度 | 产出 `docs/ops-scheduling.md` 方案 + docker-compose 加 `FEISHU_WEBHOOK` 占位；crontab 需在生产执行 |

**验证结果**：ai-audit.js 正常退出；`Blog posts: 7`（真实）；`Queue size: unknown`（诚实）；成功率从 `assumed` 变真实日志统计（测试 2 成功 1 失败 → 67%）；webhook 移除即跳过不崩。queue.ts 改动 `tsc --noEmit` 类型检查通过。

---

## 四、目标架构（简化版）

砍掉 v2 的"统一认知大脑"，改为**两条独立线 + 共享数据底座**：

```
┌─────────────────────────────────────────────────┐
│              数据底座（一切的前提）              │
│   结构化日志 + 真实指标采集 + 告警通道（飞书）   │
└──────────────┬──────────────────┬───────────────┘
               │                  │
   ┌───────────▼─────────┐  ┌────▼──────────────┐
   │   稳定性线（SRE）   │  │  增长线（业务）   │
   │ 监控→告警→预案→人工 │  │ 内容→发布→回流→迭代│
   │  传统成熟，不造 AI  │  │  AI 辅助，人工审稿 │
   └─────────────────────┘  └───────────────────┘
```

- **稳定性线**：用成熟的 SRE 传统做法（监控 + 告警 + 预案 + 人工处理），不造"自治自愈"。
- **增长线**：AI 辅助生成内容，但人工审稿是硬环节；数据回流驱动迭代。
- **数据底座**：结构化日志 + 真实指标采集。这是 v2 完全缺失的一层，却是一切的前提。

---

## 五、模块设计（按真实能力）

### 5.1 稳定性线

| 功能 | 实现 | 数据不可用时 |
|------|------|-------------|
| 健康检查 | 定时 `curl https://bookconv.com/api/health` | 报 `unreachable` + `critical` |
| 队列监控 | 连生产 Redis 读 `llen conversion:queue`（需配 `REDIS_URL`） | 报 `unknown` + `warning` |
| 转换成功率 | 从 app 结构化日志统计成功/失败 | 报 `assumed` |
| 告警分级 | L1 飞书 / L2 飞书+邮件 / L3 飞书+短信+人工 | — |

**关键约束**：L3（服务不可用）**只告警 + 提供回滚命令，不自动回滚**。回滚由人工执行。

### 5.2 增长线

- **内容生成**：`generate-blogs.js`（待修 `date`/路径 bug）+ 关键词竞争力评估（接入 GSC 数据）
- **发布**：人工审稿后入库，**不自动上线**
- **数据回流**：周报从 Google Search Console / Analytics 拉展现/点击/排名
- **迭代**：低 CTR 页面自动标记，人工优化

### 5.3 认知辅助（降级版"认知代理"）

**不是自治系统，而是 LLM 辅助工具**：

- 异常日志 → LLM 生成诊断建议（附置信度）→ 飞书推送供人工判断
- **不自动执行修复**，只提供建议和命令
- 知识积累：人工确认的诊断存入 markdown 知识库（用 `grep` 检索，不用向量库）

---

## 六、真实数据源策略（核心铁律）

| 指标 | 数据源 | 不可用时 |
|------|--------|----------|
| 博客数 | 本地文件系统 | 永远可用 |
| 队列长度 | 生产 Redis（`REDIS_URL`） | 报 `unknown` + `warning` |
| 转换成功率 | app 结构化日志 | 报 `assumed` |
| 健康状态 | `curl /api/health` | 报 `unreachable` + `critical` |
| SEO 表现 | GSC API | 跳过 SEO 模块 |

**铁律**：任何指标不可用 = 明确标注 + 降低对应置信度，**绝不伪造**。这条直接来自本次修复——v2 时代的随机数队列是反面教材。

---

## 七、KPI 基线优先

v2 直接拍"+15%/月""留存>35%"，但没有当前基线。v2.1 要求**先测 2 周当前值，再设目标**：

| 指标 | 第 1-2 周测基线 | 3 个月后目标（务实） |
|------|----------------|--------------------|
| 服务可用性 | 待测 | 99.5%（非 99.9，一人公司务实） |
| 转换成功率 | 待测 | >90% |
| MTTR | 待测 | <30min（含人工介入） |
| 自然搜索流量 | 待测 | +10%/月 |
| 博客数 | 7 | 30 |

---

## 八、路线图（基于真实地基）

### 阶段一：让现有自动化可靠（1-2 周）
- [x] 修复 `ai-audit.js`（已完成，见第三节）
- [x] 修复 `generate-blogs.js`（`date` 模板插值 + 相对路径，已验证）
- [x] app 写结构化转换日志（`queue.ts` 加 `appendConversionAuditLog`，成功率变真实统计）
- [~] 建立调度：方案已产出 `docs/ops-scheduling.md` + docker-compose 加 `FEISHU_WEBHOOK`；**crontab 需在生产服务器执行**（本地无 SSH 访问）
- [ ] 配置生产 `REDIS_URL`（docker-compose 已有 `redis://redis:6379`，生产跑即生效）
- [ ] 健康检查接入审计脚本

### 阶段二：增长线打通（3-6 周）
- [ ] `generate-blogs` 接入关键词评估（GSC）
- [ ] 内容人工审稿流程固化
- [ ] 周报自动从 GSC / Analytics 拉数据
- [ ] 邮件生命周期（**先做注册引导 2 步，非 v2 的 7 步**，验证有效再扩展）

### 阶段三：LLM 辅助诊断（7-12 周）
- [ ] 异常日志 → LLM 诊断建议 → 飞书
- [ ] 诊断知识库（markdown + grep）
- [ ] 低 CTR 页面自动标记
- [ ] **不做**：自治自愈、强化学习、向量库

---

## 九、待修地基清单（v2.1 阶段一执行项）

1. ~~generate-blogs.js `date` bug~~ ✅ 已修复（模板插值，实测真实时间戳）
2. ~~generate-blogs.js 硬编码路径~~ ✅ 已修复（相对路径）
3. **generate-blogs.js 内容质量**：中英混杂（"wait背后"）、模板单一 → 阶段二接入 LLM 改写（未做）
4. ~~调度缺失~~ 🔶 方案已产出 `docs/ops-scheduling.md` + docker-compose 占位；**crontab 待用户在生产执行**
5. ~~app 无结构化转换日志~~ ✅ 已修复（`queue.ts` `appendConversionAuditLog` 写 `conversion.log`，实测 67% 真实统计）
6. **exit code 语义**：当前 `warning` 状态退出码待明确。建议 `warning=0`（cron 不发失败告警）、`critical=1`（未做）
7. **ai-audit.js failed 计数双计**：正则 `/failed|error|exception/` 对含 "error" 的 failed 行匹配 2 次，failed 计数偏高（不影响 `rate=succeeded/totalJobs`）。待优化（未做）

---

## 十、明确不做的事（防过度工程）

| v2 提的 | v2.1 决定 | 理由 |
|---------|-----------|------|
| Kafka/Pulsar 数据管道 | ❌ 不做 | 用文件日志 + 定时拉取足够 |
| 强化学习决策树 | ❌ 不做 | 用规则 + LLM 建议 |
| 向量库 RAG | ❌ 不做 | 用 markdown 知识库 + grep |
| K8s 自动伸缩 | ❌ 不做 | 用 Docker Compose + 人工扩容 |
| 自治自愈（自动回滚/删数据） | ❌ 不做 | 人工确认，代理只建议 |
| 7 阶段邮件漏斗 | ❌ 暂不做 | 先 2 步验证有效再扩展 |
| "Agent 自动修复率>70%" KPI | ❌ 不设 | 该指标会诱导代理"假装修复"凑数 |

---

## 十一、风险与边界

| 风险 | 应对 |
|------|------|
| LLM 诊断建议不准 | 标注置信度，人工判断，不自动执行 |
| 内容质量低 | 人工审稿是硬环节，不跳过 |
| 告警疲劳 | 分级 + 静默窗口 |
| 能力边界 | 一人公司，所有"自动"都以"人工可介入"为前提 |

---

## 十二、与 v2 的核心差异总结

1. **从"统一认知大脑"到"两条独立线"**：工程稳定性走传统 SRE，业务增长走 AI 辅助流水线，解耦。
2. **从"自治自愈"到"LLM 建议 + 人工兜底"**：代理只建议不执行，高风险操作人工确认。
3. **从"假数据 All good"到"真实数据 + 诚实标注"**：不可用即 `unknown`，绝不伪造。
4. **从"先拍目标"到"先测基线"**：KPI 先量当前值。
5. **从"技术名词堆砌"到"最小可行栈"**：砍掉 Kafka/RL/向量库/K8s，用文件日志+规则+markdown。
