# 2026-08-04 工作日志

## EPUB ?TXT ?JS 轍

### 
生产 EPUB ?TXT 轍全部返回 500 INTERNAL_ERROR?
### 根因分析
1. **错映射缺失**: `error-handler.ts` 没有映射 "no text content could be extracted" 错，致返?INTERNAL_ERROR 而非 CONVERSION_FAILED
2. **g逻辑缺陷**: `conversion.ts` ?catch 块中，只有当 errorCode !== 'CONVERSION_FAILED' 时才抛出友好消息，辑反向
3. **测试文件缺陷**: 现有测试 EPUB (`valid.epub`, `valid-large.epub`) 缺少实际内文件（只?container.xml ?content.opf，没?XHTML 章节?
### 内
1. **error-handler.ts**:
   - 添加 `no text content could be extracted` ?`CONVERSION_FAILED` 映射
   - 更新友好消息：?文件參为空、损坏或无可提取文本内"
2. **conversion.ts**:
   - 移除 `if (errorCode !== 'CONVERSION_FAILED')` 条件，终为已知错码抛出友好消?3. **创建测试文件**: `tests/fixtures/test-content.epub`（含 nav.xhtml ?chapter1.xhtml?
### 验证结果
- `epub-to-txt`: **200 OK** ?成功提取文本内
- `epub-to-zip`: **200 OK** ?e透传
- `epub-to-pdf`: **500** ?预期失败（Vercel 无常驻进??Calibre?
### 提交记录
- `055b729`: fix(conversion): always throw friendly error messages for known error codes
- `6738e5f`: fix(error-handler): map epub-to-txt extraction errors to CONVERSION_FAILED

### ?- EPUB ?ZIP 格式，`epub-to-txt` 使用 jszip ?JS f，无 Calibre
- f流程：container.xml ?OPF ?spine ?按序提取 XHTML ?剥标 ?纖?- Vercel Serverless 限制：无常驻进程、无 Calibre 二进制maxDuration=60s
- 已实现：`epub-to-txt`（纯 JS）`epub-to-zip`（传?- 朮现：`epub-to-pdf` 等需?Calibre 的转捼 VPS 后?
## GSC 数据分析?026-08-04 导出，统$?07-27?8-02?- 核心?3 展示 / 0 点击 / 平均排名 ?7（ 6 页）/ CTR 0%。?85 vs 移动 8?- 趋势：展示量单调上?0?7/日），印证范域?www + canonical  + 内铺量已生效，收录推动见效?- 头部词仍沉底：mobi→epub ?69.4（KD2）epub→txt 69.2 —?页面已无叼化，卡在域名权威（链）?- 博集群已稳定拿曝光（ebook-formats-explained ?43.6 + 3 ?how-to），内/指南长尾策略奏效?- 地理 36 国美国先，验证英文优先；出现西/?德试尾命?EN 页（朝 /es 有潜力）?
## 🔴 重大发现：幽灵页 soft-404 袔录（g核实?- 根因：`KEYWORDS`（constants.ts）含 ~20 ?planned 对未?`CONVERSION_MAP`；`convert/[slug]/page.tsx` ?`generateStaticParams` 遍历 KEYWORDS 全量预渲染，?`!conversion` 时返?200「Conversion not supported」div（非 notFound，带 title/canonical ?noindex）→ soft-404 ?Google 收录?- 已收?2 丼`/convert/epub-to-zip`?2 展示、部分查询排?1 ?pos 7.5）`/convert/epub-to-lrf`?- 完整 20 幽灵对清单已记入 MEMORY.md「已?SEO 缺陷」?- 方：generateStaticParams 叔成支持 + `dynamicParams=false` + `notFound()` + UI 不链 planned 对epub→zip/lrf 建补做保留排名，其?18 ?404?- 交付：`数据分析/GSC分析_2026-08-04.md`（含完整分析+行动清单+待拍?2 件事）?
## 待用户拍?1. 昐授权直接?page.tsx 堵幽灵页（本周最?ROI ）?2. epub→zip / epub→lrf 做不做（?保留已到?1 页排名）?
## ?已执行：幽灵页修复（2026-08-04 当晚?- 用户回"按你的建讉?。实施范围：堵全?20 丹灵页（含 epub-to-zip/lrf），**?*?worker 里真做转?- 改动 5 文件?  1. `src/app/[locale]/convert/[slug]/page.tsx`：加 `export const dynamicParams = false`；`generateStaticParams` 改为 `KEYWORDS.filter(k=>getConversion(k.source,k.target))`；软 404 `return <div>` ?`notFound()`?  2. `src/app/[locale]/convert/page.tsx`：`/convert` 索引?items 过滤攌对（同时清掉 JSON-LD ItemList 里的幽灵 URL）?  3. `src/components/tools/RelatedConversions.tsx`：抽?`SUPPORTED_KEYWORDS` 过滤后给 related/others 用?  4. `src/app/[locale]/page.tsx` + `src/app/page.tsx`：页转换器网格 `filteredKeywords` 过滤攌对?- 门：`tsc --noEmit` 0 错；`seo-critic.mjs` 0/0?- 朜 `next build --webpack` ?WorkBuddy 沙 safe-delete 批量删除守卫拦截（删 ?0 文件），但已编译?.next 清理阶=编译通过；真实构建在 Vercel(Linux) 不受此限?- 提交 `7dcdf5da` 推?main ?Vercel 重建。线上验证（Node fetch，等 ~75s 部署传播）：
  - `/convert/epub-to-zip` 404 ✓`/convert/epub-to-lrf` 404 ✓`/convert/xyz-to-abc`(?slug) 404 ✓`/convert/mobi-to-epub` 200 ✓?  - **20 丹灵页现已全部g' 404**，停止稀释权?浴抓取预算?- **epub→zip/lrf 决策变更说明**：本?真做保留排名"，但核查 worker 发现 `executeConversion` 返回 `{outputFilePath,...}` 而结果路?批量跔消费 `result.base64Data`—两者间缺文件→base64 桥接（既有缺陷）。在 worker 里加 zip 透传会继承缺陷（实际下载仍 500）且这俩词当?0 点击，故稳妥选择 404 而非做假页面。建讽?*狫、经完整测试的转换功?*另排期（zip 透传机单：epub 朰?zip，制改名即叼?- **遗留 follow-up（已，下）**：sitemap.ts 硼?`CONVERSION_PAGES`(20 ? 漏了 6 东实支持页，应改为?CONVERSION_MAP 派生?
## ?已执行：sitemap 派生化修复（2026-08-04 当晚，接上条?- 用户回"好的，帮我修?。改?2 文件?  1. `src/app/sitemap.ts`：删硼?20 ?`CONVERSION_PAGES`，改 `Object.keys(CONVERSION_MAP).map(k=>{const [s,t]=k.split('-');return s+'-to-'+t})` —?与真实转换路?1:1，永不漂移?  2. `src/app/[locale]/convert/[slug]/page.tsx` ?`generateStaticParams`：从 `KEYWORDS.filter(getConversion)` 改为**直接** `Object.keys(CONVERSION_MAP).map(...)` —??sitemap 同源，杜?sitemap 列了但页?404"的错位?- 门：`tsc --noEmit` 0 错；`seo-critic.mjs` 0/0?CONVERSION_MAP 26 ?）?- 通过 Grep 计数 `tool:`  CONVERSION_MAP = 26 条（?seo-critic 致）；我手写?Node e对含 azw3/mobi 的键有?双配出21)，是脚本怙，不影响真实模块?- 提交 `073b6e0c` 推?main ?Vercel 重建。线上验证（?~80s）：
  - `/sitemap.xml` HTTP 200?5429 bytes；抽?8 ?slug（含 6 新纳?+ 2 基线）全部存圼幽灵?`epub-to-zip` 不在 sitemap ✓?- **新纳?sitemap ?6 东实页**：`rtf-to-epub`、`azw3-to-mobi`、`html-to-epub`、`doc-to-epub`、`mobi-to-pdf`、`epub-to-mobi`（之前漏收会拖慢?6 ?P0 头词的收录）?
## ?已执行：epub→zip 实现 + 下载交付桥接?026-08-04 深?- 用户回"epub→zip / epub→lrf 这个??- **🔴 关键发现（全站级 bug?*：`queue.ts` ?`executeConversion` 返回 `{outputFilePath,...}` 但结果路?`result/route.ts` line 39 强制要求 `result.base64Data` 否则 500；且原代码在 `cleanupDir(jobDir)` 删掉输出后才返回 ?**?26 丠式的下载此前全部 500 "No result available"**（站点级损坏，非仅新页）?- **（commit `c2be036`?*?  1. `queue.ts`：① `executeConversion` 改为**读输出文件→base64Data→再 cleanupDir→返?`{base64Data, extension, mimeType, fileSize}`**（与结果跔期望对齐）；?新 `isZipPassthrough`（epub→zip）分攼复制改名、跳?Calibre 与文朠验（EPUB 朰?ZIP）；?`getMimeType` ?`zip: application/zip`?  2. `conversion-map.ts`：加 `epub-zip`(tool:passthrough) + `zip` 显示名?  3. `src/data/content/epub-to-zip.ts`（英文内容页，遵待英文规则? `index.ts` 注册?  4. `public/llms.txt`：Supported Formats ?ZIP + Conversions ?EPUB to ZIP 行（同 seo-critic）?- 门：`tsc --noEmit` 0 错；`seo-critic.mjs` 0/0（CONVERSION_MAP 27 条）。提?`c2be036` 推?main ?Vercel 重建?- 线上验证（Node fetch，等 ~85s）：`/convert/epub-to-zip` 200 ✓`/convert/mobi-to-epub` 200（不回归）✓、`/convert/epub-to-lrf` 404 ✓sitemap ?`epub-to-zip` 且不?`epub-to-lrf` ✓?- **epub→lrf 决策**：Calibre 不支?LRF 输出（仅能不能写），无后 ?经用户'认后?*保持 404**，未实现?- **竈?POST 实测袲箱代理拦?*：`GET /api/convert`?05（API 叾）但 `POST`→`UND_ERR_SOCKET`（沙箱代理掐出站 POST）故真实下载用户在浏览器/非代理网络验证临时测试脚?`scripts/_e2e_epub_zip.cjs` 已移出仓库（mv ?/c/tmp）?
## 🔴🔴 重大发现：生产环境转换线整体挂死（比下?bug 更严重，2026-08-04 深?- **现象**：网络恢复后用户要求"继续测试"。实?`POST /api/convert`（epub→zip）与 `GET /api/health` **均超?*，Vercel 返回 **504 FUNCTION_INVOCATION_TIMEOUT**；?`GET /api/convert`?05（不?Redis）回三处一致：$ Redis 的路由全挂?- **根因（代码核实）**?  1. `REDIS_URL` ?Vercel 上指向一?*网络不可达的 Redis**（半仍?VPS 地址或配罔诼。`src/lib/redis.ts` ?ioredis ?`maxRetriesPerRequest:null` + 默无限重连 ?对不叾主机 `ping()/connect()` **永不 resolve** ?`/api/health` ?`Promise.all([checkRedis,...])` 永远卡 ?函数超时 504?  2. **Vercel serverless 无常?worker**：`queue.ts` ?`startWorker()` 仅在 dev 臐；Vercel Node 函数?import 触发它，但函数返回即袆结，**job 永不袶?*?  3. 结?*BullMQ+Redis+常驻 worker 这异架构?Vercel serverless 上天然不成立**；当?REDIS_URL 不可??整条 Redis 链路挂?- **影响**：生产环?*全部 26 丠式转?100% 失败**（含刚做?epub→zip）前下?base64 桥接 + zip 透传g机g'，但**当前无法袧?*（POST 在入队阶段就挂了）用户早?站看起来不太对劲"大率因?- **方向（待用户拍板，已?`诊断_轍$生产故障_2026-08-04.md`?*?  - **方 A（推荐，忏甼**：Vercel 上改?*请求内同步转?*—POST 处理器直接调 `executeConversion()` ?Calibre/zip 透传，结果内联返回（小文?`data:` base64 / 大文?R2·朜下载 URL），**彻底去掉 Redis·worker 依赖**。代价：?Vercel 函数超时约束（hobby 10s / pro 60s），超大文件參超限?  - **方 B**：后台搬到有状平台（Upstash Redis + 常驻 worker / VPS / Railway），Vercel 仅做前?  - **方 C（仅缓解不?*：只?REDIS_URL 配 ?POST 能入队返?202，但**?worker 消费** ?结果无限 202，仍不可用?  - **g层可顺带加固**：ioredis ?`maxRetriesPerRequest` 上限 + 有限 `retryStrategy`， Redis 不可达时**忟失?*而非挂 504?- **昐已动g**：本次仅做了诊断与报告（朔轍架构）等用户?A ?B 再动手诊斊告：`诊断_轍$生产故障_2026-08-04.md`?
## ?已执行：方 A（Vercel 请求内同步转捼实现 + 验证?026-08-04 深→日，用户?A"?- **用户?A**。实施：
  1. 新建 `src/lib/conversion.ts`：把纽换辑 `executeConversion`+`runConversion`(带计日? ?`queue.ts` **抽**，零 Redis/BullMQ import ?不可能在不可?Redis 上挂死?  2. 重写 `src/app/api/convert/route.ts`：去?BullMQ 入队，直?`runConversion()` 同跑，把输出字?*流式返回**（`200 + Content-Disposition`）前?`ToolPageClient` 朰?`response.blob()`，无 ?无缝对接。`export const maxDuration=60`?  3. `src/lib/queue.ts`：worker 臐?`!process.env.VERCEL` 守卫（防御：旧异步路?import 时不再连 Redis 挂）?  4. `ToolPageClient.tsx`：下载文件名改用真实盠扩展名（`book.epub`→`book.zip`）?  - 门：`tsc --noEmit` 0 错；`seo-critic.mjs` 0/0。提?`0bc2b77`?- **🔴 新发现：Calibre 不在 Vercel**：网络恢复后跑真?POST 竈竵试（`C:/tmp/e2e_sync.cjs`）：
  - `epub→zip`?*HTTP 200，合?ZIP（magic 504b0304），文件?test.zip** ?同架构 + 下载交付**全链跪证过** ✓?  - `epub→txt` / `epub→pdf`?*HTTP 500 INTERNAL_ERROR** ?二走 Calibre，?`package.json` ?Calibre 依赖（只?`Dockerfile`/`docker-compose.yml` ?VPS/Docker 镜像?`ebook-convert`），**Vercel serverless 运时根朲?`ebook-convert` 二进?* ?exec ENOENT ?500?  - 结?*方 A 俥了求链?直接返回文件」架构，?25 丵 Calibre 的格式因引擎不在 Vercel ?500**。serverless 也无?apt ?Calibre?- **收尾跾（已实现、待吔，commit `607fa10`?*?  - 新建 `src/lib/convert-handler.ts`（共?validate+convert+stream，供兼/内部跔复用）?  - `api/convert/route.ts`：若设了 `CONVERSION_BACKEND_URL`+`CONVERSION_INTERNAL_SECRET`，把上传**轏到后?`/api/convert-internal`**（密头保护）并流回响应；否则本地跑（引擎无关转捼?  - 新建 `src/app/api/convert-internal/route.ts`：密保护的内部竂，在**?Calibre ?VPS/Docker 镜像**上本地跑轍（`maxDuration=120`）?  - 线上验证：① epub→zip 重构?*?200 有效 ZIP**（无回归）✓；② `/api/convert-internal` 无密钥返?**403**（安全校验生效）✓?  - **吔步（需用户操作?*：①  VPS 活着?`ebook-convert --version` 叔；② VPS ?main 重建（含 `convert-internal` 跔）；?Vercel ?`CONVERSION_BACKEND_URL=https://<vps域名>` ?`CONVERSION_INTERNAL_SECRET=<随机?`。好后 27 丠式全通，无需再改g?- **未?*：Calibre 格式的到（需 VPS 后在线）；批量 `api/convert/batch` 仍走?queue（Vercel 上仍坏，已知 follow-up，未劼?
## 🔍 VPS 现状摸查（用户"摸一?VPS 现状"? 后接线配置准
- **结：从 AI 沙无法触达 VPS**。证捼?`bookconv.com`→`216.198.79.1`（纯 Vercel IP），兽 DNS 全在 Vercel；② `api/convert/backend/vps/worker.bookconv.com` 等子域全f到本地路由器 `192.168.31.1`（NXDOMAIN 兜底），VPS 朌任何已知子域；③ VPS 主机地址叜 GitHub Actions Secrets（`secrets.PRODUCTION_HOST`），仓库文件?IP，无 SSH 凍?- **安全核查**：`.env.production`（含真实 Lemon Squeezy 密钥）已?`.gitignore` 忽略?*朏?*，无泄露；仅 `.env.example`（模板）袿?- **已准备（g/配置侧全部就综commit `3735b0b`，本地已提交、push 因沙?TLS 握手故障暂未推上 GitHub?*?  - `docker-compose.yml`：app 服务新 `CONVERSION_INTERNAL_SECRET=${CONVERSION_INTERNAL_SECRET:-}`（VPS 重部署时?`.env` 读入）?  - `.env.example`：新?`CONVERSION_BACKEND_URL`（仅 Vercel 侧）/ `CONVERSION_INTERNAL_SECRET`（两侧一致）模板 + 架构说明?  - `DEPLOYMENT.md`：新增Vercel ?VPS 轍后接线」整节（重部?设变?验证命令）?  - `轍后接线指南.md`（新根文件）：给站长的操?runbook（含 3 ?exact 命令 + 验证）?  - `诊断_轍$生产故障_2026-08-04.md`：本 arc 的根因诊於并提交?- **待用户执行的 3 步（沙做不了）**：① `ssh VPS; cd /opt/ebook-converter; echo "CONVERSION_INTERNAL_SECRET=$(openssl rand -hex 32)" >> .env; docker compose pull app && docker compose up -d app`（校验无密钥 403）；?Vercel ?`CONVERSION_BACKEND_URL=https://<vps>` + `CONVERSION_INTERNAL_SECRET`=同串；③ `curl` ?epub→zip(朜) ?epub→txt(?VPS 委派)?- **push 状?*：commit `3735b0b` 已本地落盘；`git push origin main` 连续 5 次报 `schannel: failed to receive handshake`（沙箱代理 GitHub HTTPS 的临?TLS 故障，会话早?push e）待网络恢后重推即?- **?push 成功?026-08-04 23:07?*：用户恢复海外网络后重推成功（`607fa106..3735b0be main -> main`）Vercel 臊重建，线上验证全绿：首页 200、`/convert/epub-to-zip` 200、`/sitemap.xml` 200。`/api/health` 仍超时（已知：Redis 不可达，不影响转捼?*全部g变更已上?*?
---

## 2026-08-05 :VPS ʵ״̬ȷ

ʷ¼з VPS Ϣ:
- **IP**: 149.104.69.126
- **ʵ ID**: ecs-di00005bwn85
- **Ƴ**: Ө( Hetzner)
- **SSH Կ**: δע,ûڿ̨
- **״̬**: δĿ(˿ 80 վ,3000 ˿δ)

վǰȫ **Vercel** (bookconv.com),VPS Ϊ Calibre תԤ
