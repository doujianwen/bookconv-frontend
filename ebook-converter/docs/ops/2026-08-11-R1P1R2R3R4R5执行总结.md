# 2026-08-11 R1/R2/R3/R4/R5 完整执行总结

> 汇总自今日 GSC 诊断 → R1 → P1 → R2 → R3 → R4 → R5 全流程。
> 状态：R1-P1-R2-R4 已部署，R5 commit `4f6a13b` 待用户本地 push。

---

## 1. 触发点

用户提供 GSC 截图：**103 未索引 / 270 总量**，重点 3 类：
- 已抓取 - 尚未编入索引 ×1 → `/guide/mobi-to-epub-keep-formatting`
- 已发现 - 尚未编入索引 ×96 → `/blog/azw3-vs-mobi`（互动 216s 支柱）+ 长尾
- 备用网页 ×6 → 正常

代码侧核查结论：**非技术故障**，根因 = 新站爬取预算有限 + mobi/azw3/kindle 簇过饱和（~15+ 页争同一意图）。

---

## 2. 执行动作（按优先级）

### 🔴 P0 方案 A：重定位 `/guide/mobi-to-epub-keep-formatting`
- 并吞独有资产：4 段（转前保全清单 / 封面丢失排错 / 章节合并排错 / 存活 vs 手修对照表）+ 1 FAQ
- 打通簇内集权：`/convert/mobi-to-epub` + `/blog/mobi-to-epub`(en+es) 各加 1 条内链指向本 guide
- commit `c05b918`，已推送部署 ✅

### 🟢 P1：排除 dev/internal 页面出索引 + sitemap
- `types.ts` 新增 `noindex?: boolean`
- `env-variables-setup.ts` 标 `noindex: true`
- blog/tag 路由一律 noindex（归档页天生薄）
- sitemap 移除标签页 + 过滤 noindex 博文
- commit `25ed159`，已推送部署 ✅

### 🔴 R2：MOBI vs AZW3 三页争 → 一页吃整簇
- 胜者：`/blog/azw3-vs-mobi`（内容最丰 8段+11FAQ、hub）
- 败者：`/guide/mobi-vs-azw3` + `/blog/mobi-or-azw3-for-kindle` → 301 归档
- 并吞：7行逐型号决策表（升级原3行粗表）+ Calibre 桌面工作流 + Send to Kindle MOBI FAQ + 史实修正 2007→2005
- 踩坑：tsconfig 排除 `src/data/_archived`（归档 guide 带相对 import 导致 tsc 炸）
- commit `3a60fd9`/`a02df6d`，已推送部署 ✅

### 🟡 R3：epub→mobi 钱页 vs 指南（已治理 ✅）
- 钱页 `Convert EPUB to MOBI Online — Free Converter` + 指南 `How to Convert EPUB to MOBI Online: The BookConv Guide`
- 状态：title 修正后（commit `c81ee89`，已部署）同含精确短语但靠 "Free Converter/No Sign-up" vs "How to/Guide" 区分，结构健康（guide→money 内链）。**无需再改**

### 🟡 R4：Kindle 子簇 EPUB→AZW3 两页争 → 一页
- 胜者：`/guide/epub-to-azw3-for-kindle`（含 Calibre 桌面工作流）
- 败者：`/blog/epub-to-azw3` → 301 归档
- 并吞：What Is AZW3 / Will Formatting Survive / When to Use AZW3 / Key Takeaways 四段
- 修复胜者互链循环（败者已归档后的 301 环）
- commit `ce07086`，已推送部署 ✅

### 🟡 R5：Kindle 子簇立 kindle-formats 为支柱
- 立 `/guide/kindle-formats` 为 Kindle 子簇支柱，新增「Related guides」段（4 条卫星页内链）+「Start converting」段（钱页 CTA）
- 内链集权：5 卫星页均加强化指向支柱
  - `/blog/can-kindle-read-azw3` intro 强化指向
  - `/blog/why-ebook-wont-open-kindle` intro + 修复段强化指向
  - `/blog/mobi-to-kobo` 跨生态段加指向 `epub-to-azw3-for-kindle`
  - `/guide/epub-to-azw3-for-kindle` What Is AZW3 段改指向支柱
- 门禁：seo-critic 0/0 ✅，next build 通过 ✅
- commit `4f6a13b`，本地已 commit，push 因 sandbox `/dev/tty` 缺失导致 credential-manager 无法读取密码，**用户需在本地终端执行 `git push origin main`**

---

## 3. 门禁记录

| 门禁 | 结果 |
|---|---|
| seo-critic | 0/0（博文 32 / guide 20 / 转换 30） |
| next build --webpack | 全路由编译成功（266 路由） |
| 线上验证 | curl 确认 R2/R4 内容已渲染，R5 部分已部署（卫星页），支柱页待部署 |

---

## 4. 推送状态

本地 `main` = `4f6a13b`，远端 `origin/main` = `ce07086`，差 1 commit（R5）。
R1-R4 全部已部署。

---

## 5. 验收节点（2026-08-24）

自动化提醒已建（automation `automation-1786373788867`），到点拉 GSC 数据判：

- `/blog/azw3-vs-mobi`：查询 `azw3 vs mobi` / `mobi vs azw3` / `mobi or azw3 for kindle` 是否进前 30
- `/blog/ebook-formats-explained`：查询 `epub vs azw3 vs mobi` 是否进前 30
- `/guide/mobi-to-epub-keep-formatting`：是否从「已抓取-未编入索引」移出

---

## 6. 待办

- **用户操作**：本地执行 `git push origin main` 推送 R5（commit `4f6a13b`）
- GSC 复测后决定：是否继续 R3（Kindle 子簇剩余 5 页过饱和）/ 首页外链 / 国际版矩阵
- GA4 自定义事件（file_upload / conversion_complete）重导数据窗口含 08-10 起 → 漏斗分析
- AI 独立开发实战课 Phase 5 实现（Step D 全量实测）
