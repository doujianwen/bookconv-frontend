# 本周站内内容生成计划（2026-08-12 ~ 08-16）

> 起草：2026-08-12。数据基线：GSC 8/10 全站导出（287 展示 / 79 词 / 0 点击 / 20 页）+ SERP 审计 8/11 + 转换页收录核对清单 8/03。
> 当前内容规模：36 博客 + 21 指南 + 30 转换 = 87 页（seo-critic 注册数）。
> 适用铁律：一页吃整簇（不重复已有意图）、正文全英文禁反引号、内链只指真实 slug、seo-critic 0/0 + tsc 0 + build 通过。
> **状态说明（2026-08-12 校正）**：本计划起草后，仓库在 8/11 已由 `豆豆妈` 提交 `e76e43d`（epub-to-zip 优化+PAA FAQ）与 `a16e50e`（mobi-to-epub 内链集权），故下方轨 A 已闭环、轨 C 改走内链集权。本文件已据真实 git 状态校正。

---

## 一、数据前提（为什么这么排）

1. **0 点击危机是首要矛盾**：8/10 GSC 位置 1–20 已有 36 展示却 0 点击。GSC「搜索结果呈现」CSV 为空 → **SERP 上我们是裸链接，无任何富媒体**。这是 0 点击的结构性主因，比排名更紧迫。
2. **已排名机会簇（直接可攻）**：
   - `epub↔zip`：词层 12.5 / 页层 19.5（最接近点击）
   - `azw3 vs/→mobi`：21.4（一页吃整簇已见效）
   - `/es/blog/azw3-vs-mobi`：15.2（**全站最佳**，西语路径验证成功）
3. **沉默头部词 ~82k/月零曝光**（epub→pdf 18.1k、pdf→epub 12.1k、epub→azw3 4.4k、docx→epub 3.6k、txt→epub 2.9k、html→epub 1.9k、cbr→pdf 1.3k…）：根因是**收录 + 权威**，不是缺内容。对现有 convert 页做**内链集权**可转移支付权威、帮其起量（mobi-to-epub 集权已由 `a16e50e` 验证有效）。
4. **小词先行节奏**：新站权威≈0，先吃已排名/低 KD 词，再攻大词。

---

## 二、本周三轨（状态校正版）

### 轨 A — 抢 snippet（P0）✅ 已闭环

| 项 | 状态 | 证据 |
|---|---|---|
| **A1** `/convert/epub-to-zip` FAQ/PAA | **已完成** | commit `e76e43d`（8/11，豆豆妈）：title/meta 优化 + 加 2 条 PAA FAQ（现共 9 条：含 "How do I extract files from an EPUB without converting?" / "Can I edit the files inside the EPUB ZIP?"）。FAQSection 自动发 FAQPage JSON-LD |
| **A2** `/blog/azw3-vs-mobi` PAA FAQ | **已完成** | `azw3-vs-mobi.ts` 已有 12 条英文 FAQ + 11 条西语 FAQ（含 "Is MOBI still usable?" / "AZW3 vs KFX" 等 PAA 型），富媒体结构齐备 |

> 轨 A 是本周最高杠杆，已由既有提交收口。后续只等 **8/24 GSC 复测** 验证是否抢到 snippet。

### 轨 B — 复制西语胜势（P1）

| 项 | 状态 | 动作 | 验收 |
|---|---|---|---|
| **B1** `/es/blog/epub-vs-mobi` | **缺口（待做）** | `epub-vs-mobi.ts` 当前**无 `es` 字段**（已核实 grep 无匹配）。需补独立西语 `es` 数据（intro/sections/faqs），按 `/es/blog/azw3-vs-mobi` 模板：写 `es` 字段 → `src/data/blog/index.ts` 注册（英文版已注册）→ `messages/es.json` 同步同名键 | sitemap 派生含 `/es/blog/epub-vs-mobi`；延续西语前 20 优势 |
| **B2** `/es/convert/mobi-to-epub` | **已完成** | `mobi-to-epub.ts` 已含 `es:` 内容（grep count=2），西语交易页已存在 | sitemap 含 `/es/convert/mobi-to-epub`（已验证路由） |

> 轨 B 只剩 **B1 一个缺口**。西语页是当前全站最佳表现者，补这一页边际成本极低、确定性高。

### 轨 C — 内链集权到沉默头部词（P2，替代"新建博客"）

> **重要修正（避免内耗）**：原 C1 拟新建 `DOCX→EPUB` 博客 —— 但仓库已有指南 `docx-to-epub-self-publish`（slug `docx-to-epub-self-publish`，"Turn Your Word Manuscript into a Real Ebook"）。新建同意图博客会**违反一页吃整簇、造成内耗撞车**。故轨 C 改为**内链集权**（与 `a16e50e` 同一打法），不新建博客。

| 项 | 目标沉默头部词 | 动作 | 内链计划 | 验收 |
|---|---|---|---|---|
| **C1** 集权到 docx→epub 指南 | docx→epub 3.6k/mo（零曝光） | 从 4–6 个高排名页各加 1 条指向 `/guide/docx-to-epub-self-publish` 的内链，形成 hub-and-spoke | 选高展示页（如 `/blog/ebook-formats-explained`、`/guide/epub-to-azw3-for-kindle`、`/convert/docx-to-epub` 等），锚文本用真实词序 | seo-critic 0/0（死链门禁）；8/24 看 docx→epub 是否起量 |
| **C2** 集权到 html 相关路径 | html→epub 1.9k/mo（零曝光） | 先核实是否存在 `html→epub` 转换工具；若有则集权到该 convert 页，否则集权到 `/convert/epub-to-azw3` + `/convert/epub-to-zip`（HTML 源经 EPUB 再处理） | 同上，锚文本真实 | 同上 |

> 轨 C 通过内链把权威导向沉默 convert 页，是帮 82k 沉默词起量的内容侧杠杆（配合外链节奏），且零内耗风险。

---

## 三、排期（Aug 12–16，校正版）

| 日 | 轨 | 动作 | 门禁 |
|---|---|---|---|
| 周一/二 | A | ~~A1 + A2~~ 已闭环，仅做部署验证复核 | curl 验证（剥离 script）确认 FAQ JSON-LD 在线 |
| 周三 | B | **B1**：补 `/es/blog/epub-vs-mobi` 的 `es` 字段 + 注册 + es.json 同步 | tsc0 → seo-critic0 → build --webpack → sitemap 含 `/es/blog/epub-vs-mobi` |
| 周四/五 | C | **C1 + C2**：高排名页各加 1 条集权内链到 docx/html 路径 | 同上 + seo-critic 死链门禁（内链只指真实 slug） |

---

## 四、明确排除（不在本周范围）

- **沉默头部大词（epub→pdf 18.1k 等）新建页**：当前是收录/权威问题，先走 GSC 收录核实 + 外链，不盲目建页（建了也不排名，反而稀释）。
- **新建 DOCX→EPUB 博客**：已有指南 `docx-to-epub-self-publish` 覆盖该意图，新建会造成内耗（一页吃整簇红线）。改为内链集权。
- **国际矩阵全量**：仅激活 `/es/convert/mobi-to-epub` + 西语博客，MegaConvert 级多语暂不铺。
- **R3 Kindle 剩余 5 页过饱和**：等 8/24 GSC 复测再定是否继续合并。
- **/compat 占位内容**：Step D 待本机 Calibre 就绪后真跑，本周不部署占位数据（R1 红线）。

---

## 五、依赖与风险

1. B1 西语博客须按 `/es/blog/azw3-vs-mobi` 的 `es` 数据结构补字段，并在 `messages/es.json` 同步同名键（next-intl v4），避免 i18n 缺键告警。
2. 所有内链须过 seo-critic 死链门禁：URL 必须指向真实存在的 `/blog`、`/convert`、`/guide` slug（尤其 C1/C2 的集权目标必须已存在）。
3. 轨 A 改动现有页后需重新跑 build 确认 RSC 渲染未破（尤其 FAQPage JSON-LD 注入）——`e76e43d` 已门禁通过，仅需线上复核。
4. 完成判定节点：**8/24 自动化**拉 GSC 数据，重点看 epub-to-zip / azw3-vs-mobi 是否抢到 snippet、西语页是否延续前 20、docx/html 路径是否起量。

---

_本计划已据 2026-08-12 真实 git 状态校正（轨 A 闭环、轨 C 改内链集权），并提交 git 作为本周执行基线。每日门禁通过后部署验证。_
