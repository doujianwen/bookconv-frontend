import os, re\n\ndir_path = "./ebook-converter/src/data/content"\nfiles = [f for f in os.listdir(dir_path) if f.endswith(".ts") and f != "index.ts"]\nbad = []\nfor f in sorted(files):\n    c = open(os.path.join(dir_path, f), encoding="utf-8").read()\n    issues = []\n    bt = chr(96)\n    bs = chr(92)\n    # Check for the broken pattern: body: ` \\`content\\`\n    #     }`,\n    if bt + " " + bs+bs+bt in c:\n        issues.append("escaped-backtick-in-body")\n    if "}\`," in c or "]`\n" in c:\n        issues.append("brace-or-array-backtick")\n    if issues:\n        bad.append((f, issues))\n\nprint(f"Total files: {len(files)}")\nprint(f"Files with issues: {len(bad)}")\nfor f, issues in bad:\n    print(f"  {f}: {issues}")\n