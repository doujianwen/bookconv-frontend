# 外链工具整合索引（Backlink Tooling Index）

> 生成日期：2026-08-09（2026-08-10 更新：`ebook-converter/docs/` 已并入根 `docs/`）
> 目的：把散落在三层的外链工具 / 文档 / 记录串成一张可操作的地图，消除重复、死代码与断裂引用。
> 配套文档：`外链资源完整清单.md`（真实提交记录 + P0-P3 分层）、`外链提交报告_20260809.md`（策略与本周执行计划，位于 `docs/ops/`）。

---

## 0. 当前外链工具的真实分布（整合前）

| 位置 | 内容 | 状态 |
|------|------|------|
| `docs/ops/` | 外链作战中心：资源清单、推广方案×2、竞品分析、32KB 提交文案模板、邮件模板、周 checklist、分阶段计划、调度建议 | 大多**未跟踪**（仅部分已提交）；是外链知识的真源 |
| `scripts/`（仓库根） | 92 个脚本：侦察/提交/搜索/资源生成/项目管理 | 75 个已跟踪；**提交类脚本全部占位配置** |
| `ebook-converter/` 根 | （整合前）`submit_directories.js` + `check_sites.js` 两个孤儿脚本 | **已移走**（见 §4） |
| `ebook-converter/docs/外链提交报告_20260809.md` | 策略文档，§5 跟踪表原为空、引用路径写错 | ✅ 已并入 `docs/ops/`（2026-08-10） |

**核心问题**
1. 三层散落，没有单一入口。
2. `ebook-converter/` 根目录的 2 个脚本是父目录 `scripts/` 套件的**重复副本**，且用占位域名。
3. 策略文档引用 `docs/ops/外链资源完整清单.md` 时用了相对路径，但从 `ebook-converter/docs/` 出发解析不到（应为 `../../docs/ops/...`）——**已随 2026-08-10 目录合并解决**（文档已移至 `docs/ops/` 同级）。
4. **配置缺口（已部分修复）**：原所有"真实提交"脚本硬编码 `DOMAIN='https://yourdomain.com'`、**无一接 `bookconv.com`**。整合后 6 个真正提交的脚本已改为读 `process.env.BOOKCONV_DOMAIN`（fallback 占位）；剩余配置/检查型（betalist/beta_content/prepare_betalist/quick_check）及归档孤儿仍占位，但非直接提交路径。

---

## 1. `scripts/` 脚本分类（92 个，按用途）

### A. 外链提交（Playwright / HTTP 直投）—— ⚠️ 全部占位域名，运行前必须改配置
`auto_submit_techasoft.js`、`betalist.js`、`betalist_submit.js`、`prlog_submit.js`、`prlog_pw.js`、`prlog_api2.js`、`prlog_links.js`、`pw_*`（`pw_real_submit` / `pw_startupstash`(+2) / `pw_submitsaas` / `pw_betalist` / `pw_prlog` / `pw_future*` / `pw_effortless` / `pw_external_dirs` / `pw_goodai` / `pw_high_value` / `pw_more_dirs` / `pw_active_search` / `pw_batch`(+check) / `pw_check` / `pw_dir_check`）、`submit_ft.js`、`submit_ft_fast.js`、`submit_futuretools.js`、`update_submissions.js`

### B. 侦察 / 站点检查（找提交表单、判 Cloudflare）
`check_*`（`check_dirs` / `check_more_dirs` / `check_more_sites` / `check_forms`(+2) / `check_form_detail` / `check_accessible`(+dirs) / `check_ai_*` / `check_aitools` / `check_free_dirs` / `check_prlog_betalist` / `check_startups` / `check_submit`(+links/urls) / `check_techasoft` / `check_theresanai` / `check_toolify_future`）、`cf_*`（`cf_bypass_v2`/`v3`/`cf_firefox`/`cf_http_check`）、`find_*`（`find_forms`/`find_submit_pages`）、`quick_check` / `quick_dir_check` / `deep_check` / `cache_check` / `search_dirs.js`

### C. 搜索（SerpAPI / Brave，已 env 化，安全）
`brave_search.js` / `brave_search2.js` / `brave_v2.js`（读 `process.env.BRAVE_API_KEY`）

### D. 报告 / 分析
`final_analysis.js`、`read_report.js`、`write_report.js` / `write_report2.js`、`write_final.js`、`write_final_report.js`

### E. 资源 / Logo / OG 图生成（**非外链工具**）
`beta_content.js` / `beta_logo.js` / `convert_beta.js` / `convert_png.js` / `convert_to_png.js` / `copy_logo.js` / `create_logos.js` / `create_og.js`(+2/+3) / `check_svg.js` / `list_svgs.js` / `install_sharp.js` / `prepare_betalist.js` / `resize_logo.js`(+2/+3) / `check_magick.js` / `techasoft_detail.js`

### F. 项目管理 / 审计 / 测试（**非外链工具**）
`codex-multica-sync.js`（Multica 任务同步）、`ai-audit.js` / `audit.sh`、`test_cf_bypass.js` / `test_prlog_api.js` / `test_svg.js`

> 注：A/B/C/D 是外链工具（≈65 个）；E/F 是资产生成与通用脚本（≈27 个），混入同一 `scripts/` 目录，建议后续拆分到 `scripts/backlink/` 与 `scripts/assets/`。

---

## 2. 配置状态审计

| 指标 | 数量 | 说明 |
|------|------|------|
| 硬编码密钥（sk-/Bearer/api_key） | **0** | ✅ 安全，82740153 已 env 化 |
| 用 `process.env`（搜索/审计类） | 7 | ✅ brave_search×3、check_more_dirs、search_dirs、ai-audit、codex-sync |
| 仍用占位 `yourdomain.com` | 6 | ⚠️ 4 个配置/检查型（betalist/beta_content/prepare_betalist/quick_check）+ 2 个归档孤儿；真正提交脚本已 env 化（见 §4.4） |
| 引用真实 `bookconv` | 3 | ⚠️ 仅 `betalist.js`/`beta_content.js`/`prepare_betalist.js`，但三者**同时仍含占位域名**——即未真正接好 |

**结论**：搜索/侦察类脚本可安全运行；6 个提交脚本已 env 化，**运行前设 `BOOKCONV_DOMAIN`/`BOOKCONV_EMAIL` 即发真实域名**，缺 env 时 fallback 占位不崩。

---

## 3. 真实提交记录（摘要，完整版见 `外链资源完整清单.md`）

已成功（5）：BetaList、StartupProject、ActiveSearch、FutureTools、StartupStash
待手动（4）：ListingBott(Cloudflare)、BacklinkCRM(仅newsletter)、GrowPad(Cookie表单)、SerpMaestro(无表单)
失败（1）：ADirectory(超时/被阻止)

> 当前节奏符合记忆 6.1「首页优先」铁律：外链落地 URL 应统一指向 `https://www.bookconv.com/`，暂不内推内页。

---

## 4. 本次已执行的整合动作（2026-08-09）

1. **移走 2 个孤儿脚本**：`ebook-converter/submit_directories.js`、`ebook-converter/check_sites.js` → `scripts/_legacy_orphan_backlink/`（保留可回退，清掉项目根目录死代码；二者是父目录 `scripts/` 套件的重复副本且占位配置）。
2. **修正策略文档引用**：`ebook-converter/docs/外链提交报告_20260809.md` 中 `docs/ops/外链资源完整清单.md` 的引用路径改为正确的 `../../docs/ops/外链资源完整清单.md`。**2026-08-10：该文档已整体并入 `docs/ops/`，引用已改为同级 `./`。**
3. **填充策略文档 §5 跟踪表**：原空表已并入上面 §3 的真实记录（落地页统一首页、锚文本 BookConv）。
4. **env 化真实提交脚本（聚焦版）**：6 个真正会提交的脚本改为读 `process.env.BOOKCONV_DOMAIN` / `BOOKCONV_EMAIL`，保留占位 fallback（`auto_submit_techasoft`/`betalist_submit`/`pw_real_submit`/`pw_submit_activesearch`/`submit_ft`/`submit_futuretools`）；`node --check` 语法校验全过。配置/检查型（betalist/beta_content/prepare_betalist/quick_check）及归档孤儿暂未改。

---

## 5. 建议的后续整合（可选，待确认后再做）

- [x] 把"真实提交"脚本的 `DOMAIN`/`CONTACT_EMAIL` 改为读 `process.env.BOOKCONV_DOMAIN` / `BOOKCONV_EMAIL`，fallback 占位（6 个已改：auto_submit_techasoft/betalist_submit/pw_real_submit/pw_submit_activesearch/submit_ft/submit_futuretools）。配置/检查型未改，运行前仍需人工确认域名。
- [ ] `scripts/` 拆分：`backlink/`（A–D）、`assets/`（E）、`ops/`（F），每类加 README。
- [ ] `docs/ops/` 未跟踪文档按需 `git add`（资源清单、推广方案、文案模板是外链真源，建议纳入版本控制）。
- [ ] 资源清单记录的"待手动"4 项推进到完成，并回写跟踪表。
